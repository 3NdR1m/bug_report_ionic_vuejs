import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'bug_report_ionic_vuejs',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
